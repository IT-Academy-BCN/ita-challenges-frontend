import { PostRegisterModalComponent } from './post-register-modal.component'

describe('PostRegisterModalComponent', () => {
  it('should mount', () => {
    cy.mount(PostRegisterModalComponent)
  })
})